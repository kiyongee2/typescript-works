# React + TypeScript + Vite

## 설치 및 실행
npm create vite@latest my-vite-app

→ React 선택 (select a framework)

→ TypeScript 선택(select a variant)

cd my-vite-app

설치
npm install → node_module 폴더가 없는 경우 설치

실행
npm run dev

## macos에서 nodejs upgrade

brew install nvm

nvm install —lts